defmodule <%= Mix.Vbt.test_module_name() %>.SentryClient do
  @moduledoc false
  # credo:disable-for-this-file Credo.Check.Readability.Specs

  @doc false
  def send_event(event, _opts) do
    send(self(), {:sentry_report, event})
    :ok
  end
end
